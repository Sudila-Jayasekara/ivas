```bash
# 1. ensure ollama serve 
# 2. ensure gradeloop conda env is active
conda env list
conda activate gradeloop
# 3. run the project
cd services/python/ivas
pip install -r requirements.txt
python main.py

```